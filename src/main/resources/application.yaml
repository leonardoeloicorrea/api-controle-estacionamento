server:
  port: 8081

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/api_controle_estacionamento
    username: postgres                             
    password: adm123                               
  jpa:
    show-sql: true
    open-in-view: true
    hibernate:
      ddl-auto: update
    properties:
      jdbc:
        lob:
          non-contextual-creation: true
  output:
    ansi:
      enabled: always